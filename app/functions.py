def turn_on_device(device_name: str) -> str:
    """
    Stub for turning a device on.
    Replace with real hardware/API integration as needed.
    """
    # e.g. send MQTT message, call GPIO, etc.
    return f"✅ Turned on {device_name}"

def turn_off_device(device_name: str) -> str:
    """
    Stub for turning a device off.
    """
    return f"✅ Turned off {device_name}"